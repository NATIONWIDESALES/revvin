project_id = "olmpplfgzegzqdcznlrp"

[functions]
  [functions.auth-email-hook]
    verify_jwt = false
  [functions.process-email-queue]
    verify_jwt = true
  [functions.send-campaign]
    verify_jwt = true
  [functions.stripe-business-webhook]
    verify_jwt = false
  [functions.handle-unsubscribe]
    verify_jwt = false
  [functions.twilio-inbound-webhook]
    verify_jwt = false
  # Worker authenticates a configured x-cron-secret or exact service-role key.
  # Gateway JWT decoding must not substitute for that credential verification.
  [functions.notify-new-lead]
    verify_jwt = false
