project_id = "olmpplfgzegzqdcznlrp"

[functions]
  [functions.auth-email-hook]
    verify_jwt = false
  [functions.process-email-queue]
    verify_jwt = true
  [functions.send-campaign]
    verify_jwt = true
  [functions.stripe-business-webhook]
    verify_jwt = false
  [functions.handle-unsubscribe]
    verify_jwt = false
  [functions.twilio-inbound-webhook]
    verify_jwt = false
  # Worker authenticates a configured x-cron-secret or exact service-role key.
  # Gateway JWT decoding must not substitute for that credential verification.
  [functions.notify-new-lead]
    verify_jwt = false
  # Hourly cron worker; authenticates x-cron-secret or the service-role key.
  [functions.lifecycle-emails]
    verify_jwt = false

  # Public partner-program endpoints: no session exists when they are called.
  # Each validates and rate-limits its own input.
  [functions.partner-click]
    verify_jwt = false
  [functions.partner-apply]
    verify_jwt = false
  [functions.partner-resend-link]
    verify_jwt = false
  [functions.partner-dashboard]
    verify_jwt = false
  # Daily cron worker; authenticates x-cron-secret or the service-role key.
  [functions.partner-commissions-mature]
    verify_jwt = false
